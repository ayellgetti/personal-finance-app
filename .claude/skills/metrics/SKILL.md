---
name: metrics
description: "Define measurable success metrics."
disable-model-invocation: true
---

# /metrics

Define measurable success metrics.

For each metric include:
- metric name
- what it measures
- why it matters
- how it could be measured
- possible target only when enough context exists

Avoid vanity metrics.

---

Apply `.ai/router.md` and the repository rules in `CLAUDE.md`. For code or
product changes, use `docs/PROJECT_SPEC.md` and `docs/DEVELOPMENT_PLAN.md` as
the source of truth. If instructions conflict, repository-specific rules win.

Generated from `.ai/commands/metrics.md`. Edit that file, then run
`node .ai/scripts/sync-skills.mjs`.
