---
name: rootcause
description: "Focus specifically on identifying the underlying cause rather than symptoms."
disable-model-invocation: true
---

# /rootcause

Focus specifically on identifying the underlying cause rather than symptoms.

Use:
- evidence
- dependency chains
- failure sequence
- likely contributing factors

Then provide the most direct corrective action and verification method.

---

Apply `.ai/router.md` and the repository rules in `CLAUDE.md`. For code or
product changes, use `docs/PROJECT_SPEC.md` and `docs/DEVELOPMENT_PLAN.md` as
the source of truth. If instructions conflict, repository-specific rules win.

Generated from `.ai/commands/rootcause.md`. Edit that file, then run
`node .ai/scripts/sync-skills.mjs`.
