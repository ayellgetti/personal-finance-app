---
name: grammar
description: "Correct grammar, spelling, punctuation, and obvious language errors."
disable-model-invocation: true
---

# /grammar

Correct grammar, spelling, punctuation, and obvious language errors.

Do not substantially rewrite the message unless necessary for correctness.

---

Apply `.ai/router.md` and the repository rules in `CLAUDE.md`. For code or
product changes, use `docs/PROJECT_SPEC.md` and `docs/DEVELOPMENT_PLAN.md` as
the source of truth. If instructions conflict, repository-specific rules win.

Generated from `.ai/commands/grammar.md`. Edit that file, then run
`node .ai/scripts/sync-skills.mjs`.
