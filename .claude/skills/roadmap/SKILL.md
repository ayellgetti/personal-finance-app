---
name: roadmap
description: "Create an implementation/product roadmap."
disable-model-invocation: true
---

# /roadmap

Create an implementation/product roadmap.

Group work into logical phases.

For each phase include:
- objective
- deliverables
- dependencies
- risks
- exit criteria

Prioritize foundational dependencies before downstream features.

---

Apply `.ai/router.md` and the repository rules in `CLAUDE.md`. For code or
product changes, use `docs/PROJECT_SPEC.md` and `docs/DEVELOPMENT_PLAN.md` as
the source of truth. If instructions conflict, repository-specific rules win.

Generated from `.ai/commands/roadmap.md`. Edit that file, then run
`node .ai/scripts/sync-skills.mjs`.
